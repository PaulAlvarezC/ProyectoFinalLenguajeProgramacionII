import React from "react";
import { ScrollView } from "react-native";

export default function Template(){
    

    return(
        <ScrollView vertical>
            <Text>Template</Text>
        </ScrollView>
    );
}